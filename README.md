# git-practice-folder
1. I used "git clone " to clone the repo from github to my local system
2. I used "git status" for to know the state of the directory and it show which files are tracked and which files aren't being tracked and it show which changes have been staged